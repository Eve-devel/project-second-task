//-
  options.class - дополнительный класс;
  options.type - тип логотипа (color или grey)

mixin logo(options={})
  if options.type=='color'
    .logo.logo_type_color(class=options.class)
  else if options.type=='grey'
    .logo.logo_type_grey(class=options.class)
